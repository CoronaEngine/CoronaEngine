#pragma once

// Compatibility include. The engine runtime API is no longer owned by the
// Script system; new code should include the canonical engine header instead.
#include <corona/engine/engine_runtime_api.h>
