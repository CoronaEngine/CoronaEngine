"""Legacy embedded-Python scene model kept behind an explicit namespace.

New editor features must use the manifest-backed adapters in
:mod:`api.editor_api`; this package is only for the old scene
host and registered compatibility paths.
"""
