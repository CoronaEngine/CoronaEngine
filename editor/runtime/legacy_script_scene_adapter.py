"""Compatibility import for the historical Script Runtime adapter path.

The implementation is owned by ``script_runtime.compat``. Keep this module
for older hosts that still import the runtime path.
"""

from script_runtime.compat.legacy_scene_adapter import *  # noqa: F401,F403
